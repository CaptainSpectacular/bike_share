<%= form_for @order do |f| %>
  <%= f.label "Full Name" %>
  <%= f.text_field :purchaser_name %>

  <%= f.label "Address" %>
  <%= f.text_field :address %>

  <%= hidden_field(:user_id, current_user.id) %>
  <%=  %>

  <%= f.submit %>
<% end %>
